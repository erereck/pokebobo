export const WEEK_EVENT_RULES = Object.freeze({
  chance: 0.72,
  rushChance: 0.82,
  historyLimit: 10,
  maxCaptureBonus: 0.12,
  maxTrainingBonus: 2,
  maxForageBonus: 3,
  maxAmbushShields: 2,
});
